{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"shapiq_attribution","text":"<p>Prompt-risk classification with game-theoretic explanations. A fine-tuned DistilBERT classifier estimates the probability that a user prompt is unsafe, and shapiq explains every prediction by treating the prompt's words as players in a cooperative game \u2014 computing per-word Shapley values and pairwise interactions that show which words made the prompt risky, alone and in combination.</p>"},{"location":"#live-demo","title":"\ud83d\ude80 Live demo","text":"<p>The service runs on Google Cloud Run: https://shapiq-api-268593597387.europe-west1.run.app</p> <p>Type a prompt in the web UI to get its risk score, or explore the interactive API docs.</p>"},{"location":"#features","title":"\u2728 Features","text":"<ul> <li>Prompt-risk classification \u2014 DistilBERT fine-tuned on AdvBench,   HarmBench, WildGuardMix, ToxicChat and BeaverTails</li> <li>Explainable predictions \u2014 per-word Shapley values and pairwise   interactions via KernelSHAPIQ, served as an API endpoint</li> <li>Web interface \u2014 dependency-free single-page UI served by the same   container</li> <li>Reproducible pipeline \u2014 uv-locked environment, DVC-versioned data on   GCS, Hydra configuration, Dockerized training and serving</li> <li>Experiment tracking \u2014 PyTorch Lightning training with W&amp;B logging and   a Bayesian hyperparameter sweep</li> <li>Production monitoring \u2014 metrics, Evidently drift   dashboard, and Cloud Monitoring email alerts</li> </ul>"},{"location":"#quick-start","title":"\u26a1 Quick start","text":"<pre><code>git clone git@github.com:ssophiee/shapiq-cot-attribution.git\ncd shapiq-cot-attribution\nuv sync                # env + all locked dependencies\nuv run pytest tests/   # offline test suite \u2014 no data or model needed\n</code></pre> <p>Continue with Getting started for data access, serving and Docker.</p>"},{"location":"#at-a-glance","title":"\ud83d\udcca At a glance","text":"Model DistilBERT (sequence classification, 2 labels) Training data 5 public safety datasets, deduplicated Test suite 84 offline tests, ~10 s Load test 266 requests, 0 failures, <code>/predict</code> p99 520 ms Serving Cloud Run (2 vCPU / 2 GiB, scale-to-zero)"},{"location":"#where-to-go-next","title":"\ud83d\uddfa\ufe0f Where to go next","text":"<ul> <li>Getting started \u2014 full setup, data access, Docker</li> <li>API \u2014 endpoints and request/response examples</li> <li>Training \u2014 pipeline, hardware profiles, experiment tracking</li> <li>Monitoring \u2014 drift detection, metrics, alerting</li> <li>Project structure \u2014 repository layout</li> <li>Code reference \u2014 generated from docstrings</li> </ul>"},{"location":"api/","title":"API","text":"<p>Base URL: <code>https://shapiq-api-268593597387.europe-west1.run.app</code> (or <code>http://localhost:8000</code> when serving locally).</p> Endpoint Method Purpose <code>/predict</code> POST Risk probability + <code>risky</code>/<code>safe</code> label for a prompt <code>/attribute</code> POST Per-word Shapley values and top pairwise interactions <code>/</code> GET Single-page web UI <code>/health</code> GET Liveness probe <code>/metrics</code> GET Prometheus counters and per-endpoint latency histograms <code>/monitoring</code> GET Evidently data-drift report (training baseline vs. live traffic) <code>/docs</code> GET Interactive Swagger documentation"},{"location":"api/#predict","title":"Predict","text":"<pre><code>curl -X POST https://shapiq-api-268593597387.europe-west1.run.app/predict \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"prompt\": \"How do I bake bread?\"}'\n</code></pre> <pre><code>{\n  \"prompt\": \"How do I bake bread?\",\n  \"risk_probability\": 0.02,\n  \"label\": \"safe\"\n}\n</code></pre>"},{"location":"api/#attribute","title":"Attribute","text":"<p>Explains a prediction with KernelSHAPIQ. <code>budget</code> (8\u20132048, default 256) is the number of coalition evaluations \u2014 higher is more accurate and slower; at the default budget a call legitimately takes tens of seconds.</p> <pre><code>curl -X POST https://shapiq-api-268593597387.europe-west1.run.app/attribute \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"prompt\": \"How can I hack into my neighbors wifi?\", \"budget\": 64, \"top_interactions\": 3}'\n</code></pre> <p>The response contains the risk probability plus a Shapley value per word (which words pushed the score toward risky/safe) and the strongest pairwise word interactions.</p>"},{"location":"api/#monitoring-side-effects","title":"Monitoring side-effects","text":"<p>Every <code>/predict</code> and <code>/attribute</code> call mirrors its input statistics (<code>prompt_len</code>, <code>token_count</code>) and predicted probability to a GCS bucket via FastAPI background tasks \u2014 logging never delays the response. The drift dashboard is built from exactly these rows.</p>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li><code>uv</code> \u2014 manages Python and all dependencies</li> <li>For data/model access: a Google account with permission on the DVC bucket   (ask a maintainer for an IAM grant), plus the   Google Cloud SDK</li> </ul>"},{"location":"getting-started/#environment","title":"Environment","text":"<pre><code>git clone git@github.com:ssophiee/shapiq-cot-attribution.git\ncd shapiq-cot-attribution\nuv sync\n</code></pre> <p><code>uv sync</code> installs Python 3.13 and the exact dependency versions pinned in <code>uv.lock</code> into <code>.venv</code>, including the dev tools. The test suite works immediately \u2014 it is fully offline:</p> <pre><code>uv run pytest tests/\n</code></pre>"},{"location":"getting-started/#data-and-model","title":"Data and model","text":"<pre><code>gcloud auth application-default login\nuv run dvc pull\n</code></pre> <p>This fetches the DVC-tracked datasets and the trained model (<code>models/prompt_risk_distilbert/</code>) from <code>gs://prompt_classifier_mlops</code>.</p>"},{"location":"getting-started/#serve-locally","title":"Serve locally","text":"<pre><code>uv run uvicorn shapiq_attribution.api:app --port 8000\n</code></pre> <p>Then open http://localhost:8000 for the web UI, or <code>/docs</code> for Swagger.</p>"},{"location":"getting-started/#docker","title":"Docker","text":"<pre><code>invoke docker-build-api\ninvoke docker-run-api        # mounts ./models read-only, serves on :8000\n</code></pre> <p>See <code>DOCKER.md</code> for all images and <code>deploy/README.md</code> for the Cloud Run deployment runbook (<code>PROJECT_ID=&lt;project&gt; ./deploy/cloudrun.sh</code>).</p>"},{"location":"getting-started/#pre-commit-hooks-contributors","title":"Pre-commit hooks (contributors)","text":"<pre><code>uv run pre-commit install --install-hooks\nuv run pre-commit install --hook-type pre-push\n</code></pre> <p>On commit: file hygiene + <code>ruff check --fix</code> + <code>ruff format</code>. On push: the fast offline pytest subset.</p>"},{"location":"getting-started/#useful-commands","title":"Useful commands","text":"<pre><code>uv run invoke --list                          # full task catalog\nuv run invoke test                            # tests + coverage\nuv run invoke load-test                       # locust against the deployed API\nuv run dvc repro prepare_dataset split_data   # rebuild the dataset\nuv run ruff check . --fix &amp;&amp; uv run ruff format .\n</code></pre>"},{"location":"monitoring/","title":"Monitoring","text":"<p>Monitoring of the deployed service works in three layers.</p>"},{"location":"monitoring/#1-inputoutput-collection","title":"1. Input/output collection","text":"<p>Every <code>/predict</code> and <code>/attribute</code> call mirrors its prompt, input statistics (<code>prompt_len</code>, <code>token_count</code>) and predicted probability to a GCS bucket via FastAPI background tasks, so collection never delays responses and survives Cloud Run's ephemeral disks.</p>"},{"location":"monitoring/#2-drift-detection","title":"2. Drift detection","text":"<p><code>/monitoring</code> on the deployed service fetches the training-set baseline plus all logged prediction rows and renders an Evidently report:</p> <ul> <li>Data-drift tests on <code>prompt_len</code>, <code>token_count</code> and <code>p_risky</code></li> <li>Health test: the mean predicted risk must stay in <code>[0.2, 0.8]</code> \u2014 a   model that collapsed to predicting everything safe (or everything risky)   fails this even without input drift</li> </ul> <p>The same report can be built locally: <code>invoke monitor-report</code> (local rows) or <code>invoke monitor-report-cloud</code> (rows fetched from the bucket).</p>"},{"location":"monitoring/#3-system-metrics-and-alerting","title":"3. System metrics and alerting","text":"<ul> <li>Prometheus at <code>/metrics</code>: <code>api_requests_total</code> (per endpoint + status   code), <code>api_request_duration_seconds</code> latency histograms, and   <code>api_predicted_labels_total</code> (risky vs. safe \u2014 catches a collapsed model   operationally)</li> <li>Google Cloud Monitoring watches Cloud Run's request count and latency,   with two email alert policies (<code>deploy/alerts.sh</code>): any 5xx responses   within a 5-minute window, and p95 latency above 30 s</li> </ul> <p>This setup has already paid off once: a 5xx alert fired, the request logs showed slow cold starts (the container re-installed dev dependencies at startup), and the Docker entrypoint was fixed as a result \u2014 cutting cold starts from ~90 s to roughly the model-load time.</p>"},{"location":"reference/","title":"Code reference","text":"<p>API documentation generated from the docstrings of the core modules.</p>"},{"location":"reference/#model","title":"Model","text":""},{"location":"reference/#shapiq_attribution.model","title":"shapiq_attribution.model","text":"<p>Model utilities for prompt-risk classification.</p>"},{"location":"reference/#shapiq_attribution.model.PromptRiskPredictor","title":"PromptRiskPredictor","text":"<p>Convenience wrapper for prompt-risk probability prediction.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>class PromptRiskPredictor:\n    \"\"\"Convenience wrapper for prompt-risk probability prediction.\"\"\"\n\n    def __init__(\n        self,\n        model: torch.nn.Module,\n        tokenizer: Any,\n        max_length: int,\n        device: torch.device,\n    ) -&gt; None:\n        \"\"\"Initialize the predictor.\n\n        Args:\n            model: Prompt-risk classifier.\n            tokenizer: Prompt-risk tokenizer.\n            max_length: Maximum tokenized sequence length.\n            device: Torch device.\n        \"\"\"\n        self.model = model\n        self.tokenizer = tokenizer\n        self.max_length = max_length\n        self.device = device\n        self.model.to(self.device)\n        self.model.eval()\n\n    @classmethod\n    def from_pretrained(\n        cls,\n        model_name_or_path: str | Path,\n        max_length: int = 128,\n        device: str | torch.device = \"cpu\",\n    ) -&gt; PromptRiskPredictor:\n        \"\"\"Load a predictor from a Hugging Face model name or local model directory.\n\n        Args:\n            model_name_or_path: Hugging Face model name or local model directory.\n            max_length: Maximum tokenized sequence length.\n            device: Torch device name or device object.\n\n        Returns:\n            Prompt-risk predictor.\n        \"\"\"\n        torch_device = torch.device(device)\n        model_path = str(model_name_or_path)\n        tokenizer = load_prompt_risk_tokenizer(model_path)\n        model = load_prompt_risk_model(model_path)\n        return cls(model=model, tokenizer=tokenizer, max_length=max_length, device=torch_device)\n\n    def tokenize(self, prompt: str) -&gt; dict[str, torch.Tensor]:\n        \"\"\"Tokenize a prompt for the prompt-risk classifier.\n\n        Args:\n            prompt: Input prompt text.\n\n        Returns:\n            Tokenized model inputs on the predictor device.\n        \"\"\"\n        inputs = self.tokenizer(\n            prompt,\n            truncation=True,\n            padding=\"max_length\",\n            max_length=self.max_length,\n            return_tensors=\"pt\",\n        )\n        return {key: value.to(self.device) for key, value in inputs.items()}\n\n    def predict_proba(self, prompt: str) -&gt; float:\n        \"\"\"Predict the risky-class probability for a prompt.\n\n        Args:\n            prompt: Input prompt text.\n\n        Returns:\n            Probability assigned to the risky class.\n        \"\"\"\n        inputs = self.tokenize(prompt)\n\n        with torch.no_grad():\n            outputs = self.model(**inputs)\n            probabilities = torch.softmax(outputs.logits, dim=-1)\n\n        return float(probabilities[0, 1].detach().cpu())\n\n    def predict_proba_batch(self, prompts: list[str]) -&gt; list[float]:\n        \"\"\"Predict risky-class probabilities for many prompts in one forward pass.\n\n        Equivalent to calling :meth:`predict_proba` on each prompt \u2014 padding is\n        ignored via the attention mask, so per-prompt probabilities are\n        independent of batch composition. Padding is dynamic (to the longest\n        sequence in the batch, capped at ``max_length``) rather than a fixed\n        ``max_length``, so short coalition texts are not padded out to 128\n        tokens \u2014 this is what makes the batched pass cheaper than the loop.\n\n        Args:\n            prompts: Input prompt texts.\n\n        Returns:\n            Probability assigned to the risky class for each prompt, in order.\n        \"\"\"\n        if not prompts:\n            return []\n\n        inputs = self.tokenizer(\n            prompts,\n            truncation=True,\n            padding=\"longest\",\n            max_length=self.max_length,\n            return_tensors=\"pt\",\n        )\n        inputs = {key: value.to(self.device) for key, value in inputs.items()}\n\n        with torch.no_grad():\n            outputs = self.model(**inputs)\n            probabilities = torch.softmax(outputs.logits, dim=-1)\n\n        return probabilities[:, 1].detach().cpu().tolist()\n\n    def __call__(self, prompt: str) -&gt; float:\n        \"\"\"Predict the risky-class probability for a prompt.\n\n        Args:\n            prompt: Input prompt text.\n\n        Returns:\n            Probability assigned to the risky class.\n        \"\"\"\n        return self.predict_proba(prompt)\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.PromptRiskPredictor.__call__","title":"__call__","text":"<pre><code>__call__(prompt: str) -&gt; float\n</code></pre> <p>Predict the risky-class probability for a prompt.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>Input prompt text.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Probability assigned to the risky class.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def __call__(self, prompt: str) -&gt; float:\n    \"\"\"Predict the risky-class probability for a prompt.\n\n    Args:\n        prompt: Input prompt text.\n\n    Returns:\n        Probability assigned to the risky class.\n    \"\"\"\n    return self.predict_proba(prompt)\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.PromptRiskPredictor.__init__","title":"__init__","text":"<pre><code>__init__(model: Module, tokenizer: Any, max_length: int, device: device) -&gt; None\n</code></pre> <p>Initialize the predictor.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Prompt-risk classifier.</p> required <code>tokenizer</code> <code>Any</code> <p>Prompt-risk tokenizer.</p> required <code>max_length</code> <code>int</code> <p>Maximum tokenized sequence length.</p> required <code>device</code> <code>device</code> <p>Torch device.</p> required Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def __init__(\n    self,\n    model: torch.nn.Module,\n    tokenizer: Any,\n    max_length: int,\n    device: torch.device,\n) -&gt; None:\n    \"\"\"Initialize the predictor.\n\n    Args:\n        model: Prompt-risk classifier.\n        tokenizer: Prompt-risk tokenizer.\n        max_length: Maximum tokenized sequence length.\n        device: Torch device.\n    \"\"\"\n    self.model = model\n    self.tokenizer = tokenizer\n    self.max_length = max_length\n    self.device = device\n    self.model.to(self.device)\n    self.model.eval()\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.PromptRiskPredictor.from_pretrained","title":"from_pretrained  <code>classmethod</code>","text":"<pre><code>from_pretrained(model_name_or_path: str | Path, max_length: int = 128, device: str | device = 'cpu') -&gt; PromptRiskPredictor\n</code></pre> <p>Load a predictor from a Hugging Face model name or local model directory.</p> <p>Parameters:</p> Name Type Description Default <code>model_name_or_path</code> <code>str | Path</code> <p>Hugging Face model name or local model directory.</p> required <code>max_length</code> <code>int</code> <p>Maximum tokenized sequence length.</p> <code>128</code> <code>device</code> <code>str | device</code> <p>Torch device name or device object.</p> <code>'cpu'</code> <p>Returns:</p> Type Description <code>PromptRiskPredictor</code> <p>Prompt-risk predictor.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>@classmethod\ndef from_pretrained(\n    cls,\n    model_name_or_path: str | Path,\n    max_length: int = 128,\n    device: str | torch.device = \"cpu\",\n) -&gt; PromptRiskPredictor:\n    \"\"\"Load a predictor from a Hugging Face model name or local model directory.\n\n    Args:\n        model_name_or_path: Hugging Face model name or local model directory.\n        max_length: Maximum tokenized sequence length.\n        device: Torch device name or device object.\n\n    Returns:\n        Prompt-risk predictor.\n    \"\"\"\n    torch_device = torch.device(device)\n    model_path = str(model_name_or_path)\n    tokenizer = load_prompt_risk_tokenizer(model_path)\n    model = load_prompt_risk_model(model_path)\n    return cls(model=model, tokenizer=tokenizer, max_length=max_length, device=torch_device)\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.PromptRiskPredictor.predict_proba","title":"predict_proba","text":"<pre><code>predict_proba(prompt: str) -&gt; float\n</code></pre> <p>Predict the risky-class probability for a prompt.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>Input prompt text.</p> required <p>Returns:</p> Type Description <code>float</code> <p>Probability assigned to the risky class.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def predict_proba(self, prompt: str) -&gt; float:\n    \"\"\"Predict the risky-class probability for a prompt.\n\n    Args:\n        prompt: Input prompt text.\n\n    Returns:\n        Probability assigned to the risky class.\n    \"\"\"\n    inputs = self.tokenize(prompt)\n\n    with torch.no_grad():\n        outputs = self.model(**inputs)\n        probabilities = torch.softmax(outputs.logits, dim=-1)\n\n    return float(probabilities[0, 1].detach().cpu())\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.PromptRiskPredictor.predict_proba_batch","title":"predict_proba_batch","text":"<pre><code>predict_proba_batch(prompts: list[str]) -&gt; list[float]\n</code></pre> <p>Predict risky-class probabilities for many prompts in one forward pass.</p> <p>Equivalent to calling :meth:<code>predict_proba</code> on each prompt \u2014 padding is ignored via the attention mask, so per-prompt probabilities are independent of batch composition. Padding is dynamic (to the longest sequence in the batch, capped at <code>max_length</code>) rather than a fixed <code>max_length</code>, so short coalition texts are not padded out to 128 tokens \u2014 this is what makes the batched pass cheaper than the loop.</p> <p>Parameters:</p> Name Type Description Default <code>prompts</code> <code>list[str]</code> <p>Input prompt texts.</p> required <p>Returns:</p> Type Description <code>list[float]</code> <p>Probability assigned to the risky class for each prompt, in order.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def predict_proba_batch(self, prompts: list[str]) -&gt; list[float]:\n    \"\"\"Predict risky-class probabilities for many prompts in one forward pass.\n\n    Equivalent to calling :meth:`predict_proba` on each prompt \u2014 padding is\n    ignored via the attention mask, so per-prompt probabilities are\n    independent of batch composition. Padding is dynamic (to the longest\n    sequence in the batch, capped at ``max_length``) rather than a fixed\n    ``max_length``, so short coalition texts are not padded out to 128\n    tokens \u2014 this is what makes the batched pass cheaper than the loop.\n\n    Args:\n        prompts: Input prompt texts.\n\n    Returns:\n        Probability assigned to the risky class for each prompt, in order.\n    \"\"\"\n    if not prompts:\n        return []\n\n    inputs = self.tokenizer(\n        prompts,\n        truncation=True,\n        padding=\"longest\",\n        max_length=self.max_length,\n        return_tensors=\"pt\",\n    )\n    inputs = {key: value.to(self.device) for key, value in inputs.items()}\n\n    with torch.no_grad():\n        outputs = self.model(**inputs)\n        probabilities = torch.softmax(outputs.logits, dim=-1)\n\n    return probabilities[:, 1].detach().cpu().tolist()\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.PromptRiskPredictor.tokenize","title":"tokenize","text":"<pre><code>tokenize(prompt: str) -&gt; dict[str, torch.Tensor]\n</code></pre> <p>Tokenize a prompt for the prompt-risk classifier.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>Input prompt text.</p> required <p>Returns:</p> Type Description <code>dict[str, Tensor]</code> <p>Tokenized model inputs on the predictor device.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def tokenize(self, prompt: str) -&gt; dict[str, torch.Tensor]:\n    \"\"\"Tokenize a prompt for the prompt-risk classifier.\n\n    Args:\n        prompt: Input prompt text.\n\n    Returns:\n        Tokenized model inputs on the predictor device.\n    \"\"\"\n    inputs = self.tokenizer(\n        prompt,\n        truncation=True,\n        padding=\"max_length\",\n        max_length=self.max_length,\n        return_tensors=\"pt\",\n    )\n    return {key: value.to(self.device) for key, value in inputs.items()}\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.load_prompt_risk_model","title":"load_prompt_risk_model","text":"<pre><code>load_prompt_risk_model(model_name_or_path: str, num_labels: int = 2) -&gt; torch.nn.Module\n</code></pre> <p>Load a sequence classification model for prompt-risk classification.</p> <p>Parameters:</p> Name Type Description Default <code>model_name_or_path</code> <code>str</code> <p>Hugging Face model name or local model directory.</p> required <code>num_labels</code> <code>int</code> <p>Number of output labels.</p> <code>2</code> <p>Returns:</p> Type Description <code>Module</code> <p>A Hugging Face sequence classification model.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def load_prompt_risk_model(model_name_or_path: str, num_labels: int = 2) -&gt; torch.nn.Module:\n    \"\"\"Load a sequence classification model for prompt-risk classification.\n\n    Args:\n        model_name_or_path: Hugging Face model name or local model directory.\n        num_labels: Number of output labels.\n\n    Returns:\n        A Hugging Face sequence classification model.\n    \"\"\"\n    return AutoModelForSequenceClassification.from_pretrained(model_name_or_path, num_labels=num_labels)\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.load_prompt_risk_tokenizer","title":"load_prompt_risk_tokenizer","text":"<pre><code>load_prompt_risk_tokenizer(model_name_or_path: str) -&gt; Any\n</code></pre> <p>Load a tokenizer for prompt-risk classification.</p> <p>Parameters:</p> Name Type Description Default <code>model_name_or_path</code> <code>str</code> <p>Hugging Face model name or local model directory.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A Hugging Face tokenizer.</p> Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def load_prompt_risk_tokenizer(model_name_or_path: str) -&gt; Any:\n    \"\"\"Load a tokenizer for prompt-risk classification.\n\n    Args:\n        model_name_or_path: Hugging Face model name or local model directory.\n\n    Returns:\n        A Hugging Face tokenizer.\n    \"\"\"\n    return AutoTokenizer.from_pretrained(model_name_or_path)\n</code></pre>"},{"location":"reference/#shapiq_attribution.model.save_prompt_risk_model","title":"save_prompt_risk_model","text":"<pre><code>save_prompt_risk_model(model: Module, tokenizer: Any, output_dir: str | Path) -&gt; None\n</code></pre> <p>Save a prompt-risk model and tokenizer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Hugging Face sequence classification model.</p> required <code>tokenizer</code> <code>Any</code> <p>Hugging Face tokenizer.</p> required <code>output_dir</code> <code>str | Path</code> <p>Output directory.</p> required Source code in <code>src/shapiq_attribution/model.py</code> <pre><code>def save_prompt_risk_model(model: torch.nn.Module, tokenizer: Any, output_dir: str | Path) -&gt; None:\n    \"\"\"Save a prompt-risk model and tokenizer.\n\n    Args:\n        model: Hugging Face sequence classification model.\n        tokenizer: Hugging Face tokenizer.\n        output_dir: Output directory.\n    \"\"\"\n    model_output_dir = Path(output_dir)\n    model_output_dir.mkdir(parents=True, exist_ok=True)\n    model.save_pretrained(model_output_dir)\n    tokenizer.save_pretrained(model_output_dir)\n</code></pre>"},{"location":"reference/#data","title":"Data","text":""},{"location":"reference/#shapiq_attribution.data","title":"shapiq_attribution.data","text":"<p>Data loading and normalization for prompt-risk classification.</p>"},{"location":"reference/#shapiq_attribution.data.PromptRiskDataset","title":"PromptRiskDataset","text":"<p>               Bases: <code>Dataset</code></p> <p>Torch dataset backed by a normalized prompt-risk JSONL file.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>class PromptRiskDataset(Dataset):\n    \"\"\"Torch dataset backed by a normalized prompt-risk JSONL file.\"\"\"\n\n    def __init__(self, path: str | Path) -&gt; None:\n        \"\"\"Load examples from a JSONL file.\n\n        Args:\n            path: Input JSONL path.\n        \"\"\"\n        self.examples = load_prompt_risk_jsonl(path)\n\n    def __len__(self) -&gt; int:\n        \"\"\"Return the number of examples.\"\"\"\n        return len(self.examples)\n\n    def __getitem__(self, index: int) -&gt; PromptRiskExample:\n        \"\"\"Return one prompt-risk example by index.\"\"\"\n        return self.examples[index]\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskDataset.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index: int) -&gt; PromptRiskExample\n</code></pre> <p>Return one prompt-risk example by index.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def __getitem__(self, index: int) -&gt; PromptRiskExample:\n    \"\"\"Return one prompt-risk example by index.\"\"\"\n    return self.examples[index]\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskDataset.__init__","title":"__init__","text":"<pre><code>__init__(path: str | Path) -&gt; None\n</code></pre> <p>Load examples from a JSONL file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Input JSONL path.</p> required Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def __init__(self, path: str | Path) -&gt; None:\n    \"\"\"Load examples from a JSONL file.\n\n    Args:\n        path: Input JSONL path.\n    \"\"\"\n    self.examples = load_prompt_risk_jsonl(path)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskDataset.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Return the number of examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Return the number of examples.\"\"\"\n    return len(self.examples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskExample","title":"PromptRiskExample","text":"<p>               Bases: <code>TypedDict</code></p> <p>Normalized prompt-risk example used by Person A's training pipeline.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>class PromptRiskExample(TypedDict):\n    \"\"\"Normalized prompt-risk example used by Person A's training pipeline.\"\"\"\n\n    prompt: str\n    label: int\n    source: str\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskTextDataset","title":"PromptRiskTextDataset","text":"<p>               Bases: <code>Dataset</code></p> <p>Tokenized prompt-risk dataset for transformer training.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>class PromptRiskTextDataset(Dataset):\n    \"\"\"Tokenized prompt-risk dataset for transformer training.\"\"\"\n\n    def __init__(self, examples: list[PromptRiskExample], tokenizer: Any, max_length: int) -&gt; None:\n        \"\"\"Initialize the dataset.\n\n        Args:\n            examples: Prompt-risk examples.\n            tokenizer: Hugging Face tokenizer.\n            max_length: Maximum tokenized sequence length.\n        \"\"\"\n        self.examples = examples\n        self.tokenizer = tokenizer\n        self.max_length = max_length\n\n    def __len__(self) -&gt; int:\n        \"\"\"Return the number of examples.\"\"\"\n        return len(self.examples)\n\n    def __getitem__(self, index: int) -&gt; dict[str, torch.Tensor]:\n        \"\"\"Return one tokenized training example.\"\"\"\n        example = self.examples[index]\n        encoded = self.tokenizer(\n            example[\"prompt\"],\n            truncation=True,\n            padding=\"max_length\",\n            max_length=self.max_length,\n            return_tensors=\"pt\",\n        )\n        item = {key: value.squeeze(0) for key, value in encoded.items()}\n        item[\"labels\"] = torch.tensor(example[\"label\"], dtype=torch.long)\n        return item\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskTextDataset.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index: int) -&gt; dict[str, torch.Tensor]\n</code></pre> <p>Return one tokenized training example.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def __getitem__(self, index: int) -&gt; dict[str, torch.Tensor]:\n    \"\"\"Return one tokenized training example.\"\"\"\n    example = self.examples[index]\n    encoded = self.tokenizer(\n        example[\"prompt\"],\n        truncation=True,\n        padding=\"max_length\",\n        max_length=self.max_length,\n        return_tensors=\"pt\",\n    )\n    item = {key: value.squeeze(0) for key, value in encoded.items()}\n    item[\"labels\"] = torch.tensor(example[\"label\"], dtype=torch.long)\n    return item\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskTextDataset.__init__","title":"__init__","text":"<pre><code>__init__(examples: list[PromptRiskExample], tokenizer: Any, max_length: int) -&gt; None\n</code></pre> <p>Initialize the dataset.</p> <p>Parameters:</p> Name Type Description Default <code>examples</code> <code>list[PromptRiskExample]</code> <p>Prompt-risk examples.</p> required <code>tokenizer</code> <code>Any</code> <p>Hugging Face tokenizer.</p> required <code>max_length</code> <code>int</code> <p>Maximum tokenized sequence length.</p> required Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def __init__(self, examples: list[PromptRiskExample], tokenizer: Any, max_length: int) -&gt; None:\n    \"\"\"Initialize the dataset.\n\n    Args:\n        examples: Prompt-risk examples.\n        tokenizer: Hugging Face tokenizer.\n        max_length: Maximum tokenized sequence length.\n    \"\"\"\n    self.examples = examples\n    self.tokenizer = tokenizer\n    self.max_length = max_length\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.PromptRiskTextDataset.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Return the number of examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Return the number of examples.\"\"\"\n    return len(self.examples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.build_prompt_risk_dataset","title":"build_prompt_risk_dataset","text":"<pre><code>build_prompt_risk_dataset(raw_dir: str | Path, output_path: str | Path) -&gt; None\n</code></pre> <p>Build the processed prompt-risk dataset from local raw JSONL snapshots.</p> <p>Parameters:</p> Name Type Description Default <code>raw_dir</code> <code>str | Path</code> <p>Directory containing raw normalized JSONL snapshots.</p> required <code>output_path</code> <code>str | Path</code> <p>Output path for the processed prompt-risk dataset.</p> required Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def build_prompt_risk_dataset(raw_dir: str | Path, output_path: str | Path) -&gt; None:\n    \"\"\"Build the processed prompt-risk dataset from local raw JSONL snapshots.\n\n    Args:\n        raw_dir: Directory containing raw normalized JSONL snapshots.\n        output_path: Output path for the processed prompt-risk dataset.\n    \"\"\"\n    raw_path = Path(raw_dir)\n    examples = []\n    seen_prompts = set()\n\n    for filename in (\n        \"advbench.jsonl\",\n        \"harmbench.jsonl\",\n        \"wildguard.jsonl\",\n        \"toxicchat.jsonl\",\n        \"beavertails.jsonl\",\n    ):\n        for example in load_prompt_risk_jsonl(raw_path / filename):\n            prompt_key = example[\"prompt\"].strip().lower()\n            if prompt_key in seen_prompts:\n                continue\n            seen_prompts.add(prompt_key)\n            examples.append(example)\n\n    write_prompt_risk_jsonl(examples, output_path)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.cli","title":"cli","text":"<pre><code>cli() -&gt; None\n</code></pre> <p>Parse command-line arguments for data preparation tasks.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def cli() -&gt; None:\n    \"\"\"Parse command-line arguments for data preparation tasks.\"\"\"\n    parser = argparse.ArgumentParser(description=\"Prompt-risk data utilities.\")\n    subparsers = parser.add_subparsers(dest=\"command\", required=True)\n\n    snapshots_parser = subparsers.add_parser(\"raw-snapshots\", help=\"Create local raw JSONL snapshots.\")\n    snapshots_parser.add_argument(\"--output-dir\", default=\"data/raw\")\n    snapshots_parser.add_argument(\"--hf-token\", default=None)\n    snapshots_parser.add_argument(\"--max-samples-per-source\", type=int, default=None)\n\n    build_parser = subparsers.add_parser(\"build-dataset\", help=\"Build processed dataset from raw snapshots.\")\n    build_parser.add_argument(\"--raw-dir\", default=\"data/raw\")\n    build_parser.add_argument(\"--output-path\", default=\"data/processed/prompt_risk_dataset.jsonl\")\n\n    args = parser.parse_args()\n    if args.command == \"raw-snapshots\":\n        main(\n            output_dir=args.output_dir,\n            hf_token=args.hf_token,\n            max_samples_per_source=args.max_samples_per_source,\n        )\n    elif args.command == \"build-dataset\":\n        build_prompt_risk_dataset(raw_dir=args.raw_dir, output_path=args.output_path)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.get_harmful_examples","title":"get_harmful_examples","text":"<pre><code>get_harmful_examples(token: str, split: str = 'test') -&gt; list[dict[str, Any]]\n</code></pre> <p>Return harmful raw WildGuardMix examples for compatibility with existing pipeline code.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def get_harmful_examples(token: str, split: str = \"test\") -&gt; list[dict[str, Any]]:\n    \"\"\"Return harmful raw WildGuardMix examples for compatibility with existing pipeline code.\"\"\"\n    examples = load_wildguard(token, split)\n    return [ex for ex in examples if ex.get(\"prompt_harm_label\") == \"harmful\" and ex.get(\"prompt\")]\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_advbench","title":"load_advbench","text":"<pre><code>load_advbench(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load and normalize AdvBench from Hugging Face.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str | None</code> <p>Optional Hugging Face token for gated dataset access.</p> <code>None</code> <code>max_samples</code> <code>int | None</code> <p>Optional maximum number of examples to return.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PromptRiskExample]</code> <p>Normalized risky prompt examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_advbench(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]:\n    \"\"\"Load and normalize AdvBench from Hugging Face.\n\n    Args:\n        token: Optional Hugging Face token for gated dataset access.\n        max_samples: Optional maximum number of examples to return.\n\n    Returns:\n        Normalized risky prompt examples.\n    \"\"\"\n    rows = load_dataset(ADV_BENCH_DATASET, split=\"train\", token=token)\n    return normalize_advbench_rows(rows, max_samples=max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_beavertails","title":"load_beavertails","text":"<pre><code>load_beavertails(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load BeaverTails prompt examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_beavertails(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]:\n    \"\"\"Load BeaverTails prompt examples.\"\"\"\n    rows = load_dataset(BEAVERTAILS_DATASET, split=\"330k_train\", token=token)\n    return normalize_beavertails_rows(rows, max_samples=max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_harmbench","title":"load_harmbench","text":"<pre><code>load_harmbench(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load and normalize HarmBench from Hugging Face.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str | None</code> <p>Optional Hugging Face token for gated dataset access.</p> <code>None</code> <code>max_samples</code> <code>int | None</code> <p>Optional maximum number of examples to return.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PromptRiskExample]</code> <p>Normalized risky prompt examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_harmbench(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]:\n    \"\"\"Load and normalize HarmBench from Hugging Face.\n\n    Args:\n        token: Optional Hugging Face token for gated dataset access.\n        max_samples: Optional maximum number of examples to return.\n\n    Returns:\n        Normalized risky prompt examples.\n    \"\"\"\n    rows = load_dataset(HARM_BENCH_DATASET, HARM_BENCH_CONFIG, split=\"train\", token=token)\n    return normalize_harmbench_rows(rows, max_samples=max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_prompt_risk_jsonl","title":"load_prompt_risk_jsonl","text":"<pre><code>load_prompt_risk_jsonl(path: str | Path) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load prompt-risk examples from a JSONL file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Input JSONL path.</p> required <p>Returns:</p> Type Description <code>list[PromptRiskExample]</code> <p>Normalized prompt-risk examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_prompt_risk_jsonl(path: str | Path) -&gt; list[PromptRiskExample]:\n    \"\"\"Load prompt-risk examples from a JSONL file.\n\n    Args:\n        path: Input JSONL path.\n\n    Returns:\n        Normalized prompt-risk examples.\n    \"\"\"\n    examples = []\n    with Path(path).open(encoding=\"utf-8\") as file:\n        for line_number, line in enumerate(file, start=1):\n            if not line.strip():\n                continue\n            row = json.loads(line)\n            try:\n                examples.append(\n                    make_prompt_risk_example(\n                        prompt=row[\"prompt\"],\n                        label=row[\"label\"],\n                        source=row[\"source\"],\n                    )\n                )\n            except KeyError as error:\n                raise ValueError(f\"missing required field on line {line_number}: {error.args[0]}\") from error\n    return examples\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_toxic_chat","title":"load_toxic_chat","text":"<pre><code>load_toxic_chat(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load ToxicChat prompt examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_toxic_chat(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]:\n    \"\"\"Load ToxicChat prompt examples.\"\"\"\n    rows = load_dataset(TOXIC_CHAT_DATASET, TOXIC_CHAT_CONFIG, split=\"train\", token=token)\n    return normalize_toxic_chat_rows(rows, max_samples=max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_wildguard","title":"load_wildguard","text":"<pre><code>load_wildguard(token: str, split: str = 'test') -&gt; list[dict[str, Any]]\n</code></pre> <p>Load raw WildGuardMix examples for compatibility with existing pipeline code.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_wildguard(token: str, split: str = \"test\") -&gt; list[dict[str, Any]]:\n    \"\"\"Load raw WildGuardMix examples for compatibility with existing pipeline code.\"\"\"\n    ds = load_dataset(WILDGUARD_DATASET, WILDGUARD_CONFIG, token=token)\n    return list(ds[split])\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_wildguard_prompts","title":"load_wildguard_prompts","text":"<pre><code>load_wildguard_prompts(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load WildGuardMix prompt examples with harmful and unharmful labels.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_wildguard_prompts(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]:\n    \"\"\"Load WildGuardMix prompt examples with harmful and unharmful labels.\"\"\"\n    rows = load_dataset(WILDGUARD_DATASET, \"wildguardtrain\", split=\"train\", token=token)\n    return normalize_wildguard_prompt_rows(rows, max_samples=max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_wildguard_safe","title":"load_wildguard_safe","text":"<pre><code>load_wildguard_safe(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load and normalize safe WildGuardMix prompts from Hugging Face.</p> <p>Parameters:</p> Name Type Description Default <code>token</code> <code>str | None</code> <p>Optional Hugging Face token for gated dataset access.</p> <code>None</code> <code>max_samples</code> <code>int | None</code> <p>Optional maximum number of examples to return.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PromptRiskExample]</code> <p>Normalized not-risky prompt examples.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_wildguard_safe(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]:\n    \"\"\"Load and normalize safe WildGuardMix prompts from Hugging Face.\n\n    Args:\n        token: Optional Hugging Face token for gated dataset access.\n        max_samples: Optional maximum number of examples to return.\n\n    Returns:\n        Normalized not-risky prompt examples.\n    \"\"\"\n    rows = load_dataset(WILDGUARD_DATASET, WILDGUARD_CONFIG, split=\"test\", token=token)\n    return normalize_wildguard_safe_rows(rows, max_samples=max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.load_xstest","title":"load_xstest","text":"<pre><code>load_xstest(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Load XSTest examples for evaluation.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def load_xstest(token: str | None = None, max_samples: int | None = None) -&gt; list[PromptRiskExample]:\n    \"\"\"Load XSTest examples for evaluation.\"\"\"\n    rows = load_dataset(XSTEST_DATASET, split=\"test\", token=token)\n    return normalize_xstest_rows(rows, max_samples=max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.main","title":"main","text":"<pre><code>main(output_dir: str = 'data/raw', hf_token: str | None = None, max_samples_per_source: int | None = None) -&gt; None\n</code></pre> <p>Create local raw JSONL snapshots for DVC tracking.</p> <p>Parameters:</p> Name Type Description Default <code>output_dir</code> <code>str</code> <p>Directory for JSONL snapshots.</p> <code>'data/raw'</code> <code>hf_token</code> <code>str | None</code> <p>Optional Hugging Face token for gated dataset access.</p> <code>None</code> <code>max_samples_per_source</code> <code>int | None</code> <p>Optional maximum examples per source.</p> <code>None</code> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def main(\n    output_dir: str = \"data/raw\",\n    hf_token: str | None = None,\n    max_samples_per_source: int | None = None,\n) -&gt; None:\n    \"\"\"Create local raw JSONL snapshots for DVC tracking.\n\n    Args:\n        output_dir: Directory for JSONL snapshots.\n        hf_token: Optional Hugging Face token for gated dataset access.\n        max_samples_per_source: Optional maximum examples per source.\n    \"\"\"\n    written_paths = prepare_raw_snapshots(\n        output_dir=output_dir,\n        hf_token=hf_token,\n        max_samples_per_source=max_samples_per_source,\n    )\n    for source, path in written_paths.items():\n        print(f\"{source}: {path}\")\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.make_prompt_risk_example","title":"make_prompt_risk_example","text":"<pre><code>make_prompt_risk_example(prompt: str, label: int, source: str) -&gt; PromptRiskExample\n</code></pre> <p>Create and validate a normalized prompt-risk example.</p> <p>Parameters:</p> Name Type Description Default <code>prompt</code> <code>str</code> <p>User prompt text.</p> required <code>label</code> <code>int</code> <p>Binary label where 1 means risky and 0 means not risky.</p> required <code>source</code> <code>str</code> <p>Dataset source name.</p> required <p>Returns:</p> Type Description <code>PromptRiskExample</code> <p>A normalized prompt-risk example.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If prompt, label, or source is invalid.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def make_prompt_risk_example(prompt: str, label: int, source: str) -&gt; PromptRiskExample:\n    \"\"\"Create and validate a normalized prompt-risk example.\n\n    Args:\n        prompt: User prompt text.\n        label: Binary label where 1 means risky and 0 means not risky.\n        source: Dataset source name.\n\n    Returns:\n        A normalized prompt-risk example.\n\n    Raises:\n        ValueError: If prompt, label, or source is invalid.\n    \"\"\"\n    normalized_prompt = prompt.strip()\n    normalized_source = source.strip()\n    if not normalized_prompt:\n        raise ValueError(\"prompt must be a non-empty string\")\n    if label not in {0, 1}:\n        raise ValueError(\"label must be 0 or 1\")\n    if not normalized_source:\n        raise ValueError(\"source must be a non-empty string\")\n    return {\"prompt\": normalized_prompt, \"label\": label, \"source\": normalized_source}\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.normalize_advbench_rows","title":"normalize_advbench_rows","text":"<pre><code>normalize_advbench_rows(rows: Iterable[Mapping[str, Any]], max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Normalize AdvBench rows as risky prompt examples.</p> <p>Parameters:</p> Name Type Description Default <code>rows</code> <code>Iterable[Mapping[str, Any]]</code> <p>Raw AdvBench rows.</p> required <code>max_samples</code> <code>int | None</code> <p>Optional maximum number of examples to return.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PromptRiskExample]</code> <p>Prompt-risk examples with label 1.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def normalize_advbench_rows(\n    rows: Iterable[Mapping[str, Any]],\n    max_samples: int | None = None,\n) -&gt; list[PromptRiskExample]:\n    \"\"\"Normalize AdvBench rows as risky prompt examples.\n\n    Args:\n        rows: Raw AdvBench rows.\n        max_samples: Optional maximum number of examples to return.\n\n    Returns:\n        Prompt-risk examples with label 1.\n    \"\"\"\n    examples = []\n    prompt_fields = (\"prompt\", \"goal\", \"instruction\", \"instructions\", \"content\")\n    for row in rows:\n        prompt = _first_text_value(row, prompt_fields)\n        if prompt is not None:\n            examples.append(make_prompt_risk_example(prompt=prompt, label=1, source=\"advbench\"))\n    return _limit_examples(examples, max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.normalize_beavertails_rows","title":"normalize_beavertails_rows","text":"<pre><code>normalize_beavertails_rows(rows: Iterable[Mapping[str, Any]], max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Normalize BeaverTails rows using prompt and is_safe labels.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def normalize_beavertails_rows(\n    rows: Iterable[Mapping[str, Any]],\n    max_samples: int | None = None,\n) -&gt; list[PromptRiskExample]:\n    \"\"\"Normalize BeaverTails rows using prompt and is_safe labels.\"\"\"\n    examples = []\n    for row in rows:\n        prompt = _first_text_value(row, (\"prompt\",))\n        if prompt is None or \"is_safe\" not in row:\n            continue\n        label = 0 if bool(row[\"is_safe\"]) else 1\n        examples.append(make_prompt_risk_example(prompt=prompt, label=label, source=\"beavertails\"))\n    return _limit_examples(examples, max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.normalize_harmbench_rows","title":"normalize_harmbench_rows","text":"<pre><code>normalize_harmbench_rows(rows: Iterable[Mapping[str, Any]], max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Normalize HarmBench rows as risky prompt examples.</p> <p>Parameters:</p> Name Type Description Default <code>rows</code> <code>Iterable[Mapping[str, Any]]</code> <p>Raw HarmBench rows.</p> required <code>max_samples</code> <code>int | None</code> <p>Optional maximum number of examples to return.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PromptRiskExample]</code> <p>Prompt-risk examples with label 1.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def normalize_harmbench_rows(\n    rows: Iterable[Mapping[str, Any]],\n    max_samples: int | None = None,\n) -&gt; list[PromptRiskExample]:\n    \"\"\"Normalize HarmBench rows as risky prompt examples.\n\n    Args:\n        rows: Raw HarmBench rows.\n        max_samples: Optional maximum number of examples to return.\n\n    Returns:\n        Prompt-risk examples with label 1.\n    \"\"\"\n    examples = []\n    prompt_fields = (\"Behavior\", \"behavior\", \"prompt\", \"goal\", \"instruction\")\n    for row in rows:\n        prompt = _first_text_value(row, prompt_fields)\n        if prompt is not None:\n            examples.append(make_prompt_risk_example(prompt=prompt, label=1, source=\"harmbench\"))\n    return _limit_examples(examples, max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.normalize_toxic_chat_rows","title":"normalize_toxic_chat_rows","text":"<pre><code>normalize_toxic_chat_rows(rows: Iterable[Mapping[str, Any]], max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Normalize ToxicChat rows from toxicity and jailbreaking labels.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def normalize_toxic_chat_rows(\n    rows: Iterable[Mapping[str, Any]],\n    max_samples: int | None = None,\n) -&gt; list[PromptRiskExample]:\n    \"\"\"Normalize ToxicChat rows from toxicity and jailbreaking labels.\"\"\"\n    examples = []\n    for row in rows:\n        prompt = _first_text_value(row, (\"user_input\",))\n        if prompt is None:\n            continue\n        toxicity = int(row.get(\"toxicity\", 0) or 0)\n        jailbreaking = int(row.get(\"jailbreaking\", 0) or 0)\n        label = 1 if toxicity == 1 or jailbreaking == 1 else 0\n        examples.append(make_prompt_risk_example(prompt=prompt, label=label, source=\"toxicchat\"))\n    return _limit_examples(examples, max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.normalize_wildguard_prompt_rows","title":"normalize_wildguard_prompt_rows","text":"<pre><code>normalize_wildguard_prompt_rows(rows: Iterable[Mapping[str, Any]], max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Normalize WildGuardMix rows using prompt harmfulness labels.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def normalize_wildguard_prompt_rows(\n    rows: Iterable[Mapping[str, Any]],\n    max_samples: int | None = None,\n) -&gt; list[PromptRiskExample]:\n    \"\"\"Normalize WildGuardMix rows using prompt harmfulness labels.\"\"\"\n    examples = []\n    for row in rows:\n        prompt = _first_text_value(row, (\"prompt\",))\n        label_value = str(row.get(\"prompt_harm_label\", \"\")).strip().lower()\n        if prompt is None or label_value not in {\"harmful\", \"unharmful\"}:\n            continue\n        label = 1 if label_value == \"harmful\" else 0\n        examples.append(make_prompt_risk_example(prompt=prompt, label=label, source=\"wildguard\"))\n    return _limit_examples(examples, max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.normalize_wildguard_safe_rows","title":"normalize_wildguard_safe_rows","text":"<pre><code>normalize_wildguard_safe_rows(rows: Iterable[Mapping[str, Any]], max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Normalize safe WildGuard rows as not-risky prompt examples.</p> <p>Parameters:</p> Name Type Description Default <code>rows</code> <code>Iterable[Mapping[str, Any]]</code> <p>Raw WildGuardMix rows.</p> required <code>max_samples</code> <code>int | None</code> <p>Optional maximum number of examples to return.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PromptRiskExample]</code> <p>Prompt-risk examples with label 0.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def normalize_wildguard_safe_rows(\n    rows: Iterable[Mapping[str, Any]],\n    max_samples: int | None = None,\n) -&gt; list[PromptRiskExample]:\n    \"\"\"Normalize safe WildGuard rows as not-risky prompt examples.\n\n    Args:\n        rows: Raw WildGuardMix rows.\n        max_samples: Optional maximum number of examples to return.\n\n    Returns:\n        Prompt-risk examples with label 0.\n    \"\"\"\n    examples = []\n    safe_labels = {\"benign\", \"safe\", \"unharmful\", \"not harmful\", \"not_harmful\"}\n    for row in rows:\n        prompt = _first_text_value(row, (\"prompt\",))\n        prompt_harm_label = str(row.get(\"prompt_harm_label\", \"\")).strip().lower()\n        prompt_type = str(row.get(\"prompt_type\", \"\")).strip().lower()\n        if prompt is not None and (prompt_harm_label in safe_labels or prompt_type == \"prompt_safe\"):\n            examples.append(make_prompt_risk_example(prompt=prompt, label=0, source=\"wildguard\"))\n    return _limit_examples(examples, max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.normalize_xstest_rows","title":"normalize_xstest_rows","text":"<pre><code>normalize_xstest_rows(rows: Iterable[Mapping[str, Any]], max_samples: int | None = None) -&gt; list[PromptRiskExample]\n</code></pre> <p>Normalize XSTest rows as an evaluation set.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def normalize_xstest_rows(\n    rows: Iterable[Mapping[str, Any]],\n    max_samples: int | None = None,\n) -&gt; list[PromptRiskExample]:\n    \"\"\"Normalize XSTest rows as an evaluation set.\"\"\"\n    examples = []\n    for row in rows:\n        prompt = _first_text_value(row, (\"prompt\",))\n        label_value = str(row.get(\"label\", row.get(\"type\", \"\"))).strip().lower()\n        if prompt is None:\n            continue\n        if \"safe\" in label_value and \"unsafe\" not in label_value:\n            label = 0\n        elif \"unsafe\" in label_value:\n            label = 1\n        else:\n            continue\n        examples.append(make_prompt_risk_example(prompt=prompt, label=label, source=\"xstest\"))\n    return _limit_examples(examples, max_samples)\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.prepare_raw_snapshots","title":"prepare_raw_snapshots","text":"<pre><code>prepare_raw_snapshots(output_dir: str | Path = 'data/raw', hf_token: str | None = None, max_samples_per_source: int | None = None) -&gt; dict[str, Path]\n</code></pre> <p>Download and write raw normalized JSONL snapshots for DVC tracking.</p> <p>Parameters:</p> Name Type Description Default <code>output_dir</code> <code>str | Path</code> <p>Directory for JSONL snapshots.</p> <code>'data/raw'</code> <code>hf_token</code> <code>str | None</code> <p>Optional Hugging Face token for gated dataset access.</p> <code>None</code> <code>max_samples_per_source</code> <code>int | None</code> <p>Optional maximum examples per source.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Path]</code> <p>Mapping from source name to written JSONL path.</p> Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def prepare_raw_snapshots(\n    output_dir: str | Path = \"data/raw\",\n    hf_token: str | None = None,\n    max_samples_per_source: int | None = None,\n) -&gt; dict[str, Path]:\n    \"\"\"Download and write raw normalized JSONL snapshots for DVC tracking.\n\n    Args:\n        output_dir: Directory for JSONL snapshots.\n        hf_token: Optional Hugging Face token for gated dataset access.\n        max_samples_per_source: Optional maximum examples per source.\n\n    Returns:\n        Mapping from source name to written JSONL path.\n    \"\"\"\n    output_path = Path(output_dir)\n    token = hf_token or os.environ.get(\"HF_TOKEN\")\n    snapshots = {\n        \"advbench\": (\n            load_advbench(token=token, max_samples=max_samples_per_source),\n            output_path / \"advbench.jsonl\",\n        ),\n        \"harmbench\": (\n            load_harmbench(token=token, max_samples=max_samples_per_source),\n            output_path / \"harmbench.jsonl\",\n        ),\n        \"wildguard\": (\n            load_wildguard_prompts(token=token, max_samples=25000),\n            output_path / \"wildguard.jsonl\",\n        ),\n        \"toxicchat\": (\n            load_toxic_chat(token=token, max_samples=10000),\n            output_path / \"toxicchat.jsonl\",\n        ),\n        \"beavertails\": (\n            load_beavertails(token=token, max_samples=12000),\n            output_path / \"beavertails.jsonl\",\n        ),\n        \"xstest\": (\n            load_xstest(token=token),\n            output_path / \"xstest_eval.jsonl\",\n        ),\n    }\n    written_paths = {}\n    for source, (examples, path) in snapshots.items():\n        write_prompt_risk_jsonl(examples, path)\n        written_paths[source] = path\n    return written_paths\n</code></pre>"},{"location":"reference/#shapiq_attribution.data.write_prompt_risk_jsonl","title":"write_prompt_risk_jsonl","text":"<pre><code>write_prompt_risk_jsonl(examples: Iterable[PromptRiskExample], path: str | Path) -&gt; None\n</code></pre> <p>Write prompt-risk examples to a JSONL file.</p> <p>Parameters:</p> Name Type Description Default <code>examples</code> <code>Iterable[PromptRiskExample]</code> <p>Normalized prompt-risk examples.</p> required <code>path</code> <code>str | Path</code> <p>Output JSONL path.</p> required Source code in <code>src/shapiq_attribution/data.py</code> <pre><code>def write_prompt_risk_jsonl(examples: Iterable[PromptRiskExample], path: str | Path) -&gt; None:\n    \"\"\"Write prompt-risk examples to a JSONL file.\n\n    Args:\n        examples: Normalized prompt-risk examples.\n        path: Output JSONL path.\n    \"\"\"\n    output_path = Path(path)\n    output_path.parent.mkdir(parents=True, exist_ok=True)\n    with output_path.open(\"w\", encoding=\"utf-8\") as file:\n        for example in examples:\n            validated = make_prompt_risk_example(\n                prompt=example[\"prompt\"],\n                label=example[\"label\"],\n                source=example[\"source\"],\n            )\n            file.write(f\"{json.dumps(validated, ensure_ascii=False)}\\n\")\n</code></pre>"},{"location":"reference/#evaluation","title":"Evaluation","text":""},{"location":"reference/#shapiq_attribution.evaluate","title":"shapiq_attribution.evaluate","text":"<p>Evaluation utilities for prompt-risk classification.</p>"},{"location":"reference/#shapiq_attribution.evaluate.compute_metrics","title":"compute_metrics","text":"<pre><code>compute_metrics(labels: list[int], predictions: list[int], probabilities: list[float]) -&gt; dict[str, float]\n</code></pre> <p>Compute binary classification metrics.</p> <p>Parameters:</p> Name Type Description Default <code>labels</code> <code>list[int]</code> <p>True binary labels.</p> required <code>predictions</code> <code>list[int]</code> <p>Predicted binary labels.</p> required <code>probabilities</code> <code>list[float]</code> <p>Predicted probability for the risky class.</p> required <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>Binary classification metrics.</p> Source code in <code>src/shapiq_attribution/evaluate.py</code> <pre><code>def compute_metrics(labels: list[int], predictions: list[int], probabilities: list[float]) -&gt; dict[str, float]:\n    \"\"\"Compute binary classification metrics.\n\n    Args:\n        labels: True binary labels.\n        predictions: Predicted binary labels.\n        probabilities: Predicted probability for the risky class.\n\n    Returns:\n        Binary classification metrics.\n    \"\"\"\n    metrics = {\n        \"accuracy\": accuracy_score(labels, predictions),\n        \"precision\": precision_score(labels, predictions, zero_division=0),\n        \"recall\": recall_score(labels, predictions, zero_division=0),\n        \"f1\": f1_score(labels, predictions, zero_division=0),\n    }\n    if len(set(labels)) &gt; 1:\n        metrics[\"roc_auc\"] = roc_auc_score(labels, probabilities)\n    return {key: float(value) for key, value in metrics.items()}\n</code></pre>"},{"location":"reference/#shapiq_attribution.evaluate.evaluate_model","title":"evaluate_model","text":"<pre><code>evaluate_model(model: Module, dataloader: DataLoader, device: device) -&gt; dict[str, float]\n</code></pre> <p>Evaluate a prompt-risk classifier.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Sequence classification model.</p> required <code>dataloader</code> <code>DataLoader</code> <p>Evaluation dataloader.</p> required <code>device</code> <code>device</code> <p>Evaluation device.</p> required <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>Loss and classification metrics.</p> Source code in <code>src/shapiq_attribution/evaluate.py</code> <pre><code>def evaluate_model(model: torch.nn.Module, dataloader: DataLoader, device: torch.device) -&gt; dict[str, float]:\n    \"\"\"Evaluate a prompt-risk classifier.\n\n    Args:\n        model: Sequence classification model.\n        dataloader: Evaluation dataloader.\n        device: Evaluation device.\n\n    Returns:\n        Loss and classification metrics.\n    \"\"\"\n    model.eval()\n    losses = []\n    labels = []\n    predictions = []\n    probabilities = []\n\n    with torch.no_grad():\n        for batch in dataloader:\n            batch = {key: value.to(device) for key, value in batch.items()}\n            outputs = model(**batch)\n            logits = outputs.logits\n            probs = torch.softmax(logits, dim=-1)[:, 1]\n\n            losses.append(float(outputs.loss.detach().cpu()))\n            labels.extend(batch[\"labels\"].detach().cpu().tolist())\n            predictions.extend(torch.argmax(logits, dim=-1).detach().cpu().tolist())\n            probabilities.extend(probs.detach().cpu().tolist())\n\n    metrics = compute_metrics(labels=labels, predictions=predictions, probabilities=probabilities)\n    metrics[\"loss\"] = float(np.mean(losses)) if losses else 0.0\n    return metrics\n</code></pre>"},{"location":"reference/#attribution-game","title":"Attribution game","text":""},{"location":"reference/#shapiq_attribution.game","title":"shapiq_attribution.game","text":"<p>Shapiq game layer \u2014 this is where players/coalition logic lives and will evolve.</p>"},{"location":"reference/#shapiq_attribution.game.parse_cot_steps","title":"parse_cot_steps","text":"<pre><code>parse_cot_steps(generated_text: str) -&gt; tuple[list[str], str]\n</code></pre> <p>Extract 'Step N: ...' lines and the Answer line from generated text.</p> <p>Returns:</p> Name Type Description <code>steps</code> <code>list[str]</code> <p>list of reasoning step strings (Answer line excluded)</p> <code>target</code> <code>str</code> <p>the answer string (empty string if not found)</p> Source code in <code>src/shapiq_attribution/game.py</code> <pre><code>def parse_cot_steps(generated_text: str) -&gt; tuple[list[str], str]:\n    \"\"\"Extract 'Step N: ...' lines and the Answer line from generated text.\n\n    Returns:\n        steps:  list of reasoning step strings (Answer line excluded)\n        target: the answer string (empty string if not found)\n    \"\"\"\n    all_steps = [s for s in re.findall(r\"Step \\d+:.*\", generated_text) if not re.match(r\"Step \\d+:\\s*\\.{0,3}\\s*$\", s)]\n    steps = [s for s in all_steps if \"Answer:\" not in s]\n    answer_match = re.search(r\"Answer:(.*)\", generated_text)\n    target = answer_match.group(1).strip() if answer_match else \"\"\n    return steps, target\n</code></pre>"},{"location":"reference/#shapiq_attribution.game.run_shapiq","title":"run_shapiq","text":"<pre><code>run_shapiq(cot_steps: list[str], value_fn) -&gt; tuple\n</code></pre> <p>Run ExactComputer for SV (order=1) and k-SII (order=2).</p> <p>Returns:</p> Name Type Description <code>sv</code> <code>tuple</code> <p>first-order Shapley values</p> <code>ksii</code> <code>tuple</code> <p>pairwise k-SII interactions</p> <code>baseline</code> <code>tuple</code> <p>empty-coalition score</p> Source code in <code>src/shapiq_attribution/game.py</code> <pre><code>def run_shapiq(cot_steps: list[str], value_fn) -&gt; tuple:\n    \"\"\"Run ExactComputer for SV (order=1) and k-SII (order=2).\n\n    Returns:\n        sv:       first-order Shapley values\n        ksii:     pairwise k-SII interactions\n        baseline: empty-coalition score\n    \"\"\"\n    n = len(cot_steps)\n    baseline = value_fn(np.zeros((1, n), dtype=bool))[0]\n    print(f\"Empty coalition baseline: {baseline:.4f}\")\n    game = CoTGame(n_players=n, value_fn=value_fn)\n    game.normalization_value = baseline\n    computer = shapiq.ExactComputer(n_players=n, game=game)\n    sv = computer(index=\"SV\", order=1)\n    ksii = computer(index=\"k-SII\", order=2)\n    return sv, ksii, baseline\n</code></pre>"},{"location":"reference/#value-function","title":"Value function","text":""},{"location":"reference/#shapiq_attribution.attribution","title":"shapiq_attribution.attribution","text":"<p>Stable ML core \u2014 input/output contract must not change.</p> <p>build_prompt:           (str, str, str, list[str], tokenizer) -&gt; str compute_answer_logprob: (str, str, model, tokenizer)          -&gt; float make_value_function:    (...) -&gt; callable[[np.ndarray], np.ndarray]</p>"},{"location":"reference/#shapiq_attribution.attribution.compute_answer_logprob","title":"compute_answer_logprob","text":"<pre><code>compute_answer_logprob(prompt: str, target: str, model, tokenizer) -&gt; float\n</code></pre> <p>Mean log-probability of target tokens given prompt as context.</p> Source code in <code>src/shapiq_attribution/attribution.py</code> <pre><code>@torch.no_grad()\ndef compute_answer_logprob(prompt: str, target: str, model, tokenizer) -&gt; float:\n    \"\"\"Mean log-probability of target tokens given prompt as context.\"\"\"\n    full_ids = tokenizer(prompt + target, return_tensors=\"pt\").input_ids.to(model.device)\n    prompt_len = tokenizer(prompt, return_tensors=\"pt\").input_ids.shape[1]\n    logits = model(full_ids).logits\n    log_probs = torch.log_softmax(logits[0], dim=-1)\n    answer_ids = full_ids[0, prompt_len:]\n    token_lp = log_probs[prompt_len - 1 : prompt_len - 1 + len(answer_ids)]\n    token_lp = token_lp[torch.arange(len(answer_ids)), answer_ids]\n    return token_lp.mean().item()\n</code></pre>"},{"location":"reference/#shapiq_attribution.attribution.make_value_function","title":"make_value_function","text":"<pre><code>make_value_function(question: str, few_shot: str, system: str, cot_steps: list[str], target_answer: str, model, tokenizer)\n</code></pre> <p>Returns a coalition value function for use with CoTGame.</p> Source code in <code>src/shapiq_attribution/attribution.py</code> <pre><code>def make_value_function(\n    question: str,\n    few_shot: str,\n    system: str,\n    cot_steps: list[str],\n    target_answer: str,\n    model,\n    tokenizer,\n):\n    \"\"\"Returns a coalition value function for use with CoTGame.\"\"\"\n\n    def value_fn(coalitions: np.ndarray) -&gt; np.ndarray:\n        scores = []\n        for coalition in coalitions:\n            present = [cot_steps[i] for i, m in enumerate(coalition) if m]\n            prompt = build_prompt(question, few_shot, system, present, tokenizer)\n            scores.append(compute_answer_logprob(prompt, target_answer, model, tokenizer))\n        return np.array(scores)\n\n    return value_fn\n</code></pre>"},{"location":"structure/","title":"Project structure","text":"<pre><code>shapiq-cot-attribution/\n\u251c\u2500\u2500 src/shapiq_attribution/      # the Python package\n\u2502   \u251c\u2500\u2500 api.py                   # FastAPI app: /predict, /attribute, /monitoring, /metrics\n\u2502   \u251c\u2500\u2500 web.py                   # embedded single-page frontend (served at /)\n\u2502   \u251c\u2500\u2500 model.py                 # PromptRiskPredictor + load/save utilities\n\u2502   \u251c\u2500\u2500 data.py                  # dataset download, normalization, JSONL I/O, CLI\n\u2502   \u251c\u2500\u2500 split_data.py            # train/val/test splitting\n\u2502   \u251c\u2500\u2500 train.py                 # Hydra-driven Lightning training entrypoint\n\u2502   \u251c\u2500\u2500 lightning_module.py      # LightningModule (model, loss, metrics)\n\u2502   \u251c\u2500\u2500 lightning_data_module.py # LightningDataModule (splits, dataloaders)\n\u2502   \u251c\u2500\u2500 evaluate.py              # classification metrics\n\u2502   \u251c\u2500\u2500 game.py                  # shapiq game layer (CoTGame, ExactComputer)\n\u2502   \u251c\u2500\u2500 attribution.py           # coalition value function (stable ML core)\n\u2502   \u251c\u2500\u2500 safety_analysis.py       # token-level safety attribution game\n\u2502   \u251c\u2500\u2500 monitoring.py            # drift baseline, row collection, Evidently report\n\u2502   \u251c\u2500\u2500 pipeline.py              # natural-reasoning CoT attribution (research)\n\u2502   \u2514\u2500\u2500 visualize.py             # attribution plots\n\u251c\u2500\u2500 tests/                       # 84 offline tests (stubbed models/tokenizers)\n\u251c\u2500\u2500 configs/                     # Hydra configs: train.yaml, sweep.yaml, hardware/\n\u251c\u2500\u2500 data/                        # DVC-tracked (raw snapshots, processed dataset)\n\u251c\u2500\u2500 models/                      # DVC-tracked trained model\n\u251c\u2500\u2500 dockerfiles/                 # api / train / train.gpu images\n\u251c\u2500\u2500 Dockerfile                   # Cloud Run image (model baked in)\n\u251c\u2500\u2500 deploy/                      # cloudrun.sh, alerts.sh, runbook\n\u251c\u2500\u2500 experiments/                 # attribution experiment runner (W&amp;B)\n\u251c\u2500\u2500 docs/                        # this MkDocs site\n\u251c\u2500\u2500 reports/                     # exam report + figures\n\u251c\u2500\u2500 dvc.yaml / dvc.lock          # data pipeline definition\n\u251c\u2500\u2500 tasks.py                     # invoke task catalog\n\u251c\u2500\u2500 locustfile.py                # load test\n\u2514\u2500\u2500 pyproject.toml / uv.lock     # dependencies (uv)\n</code></pre>"},{"location":"structure/#ci-github-actions","title":"CI (GitHub Actions)","text":"<ul> <li><code>ci-lint.yaml</code> \u2014 ruff lint + format check</li> <li><code>ci-api.yaml</code> / <code>ci-train.yaml</code> \u2014 tests + Docker image builds for the API   and training images on every push/PR to <code>main</code></li> <li><code>monitoring.yaml</code> \u2014 drift-report tooling</li> </ul>"},{"location":"training/","title":"Training","text":""},{"location":"training/#pipeline","title":"Pipeline","text":"<p>The Hydra/DVC pipeline splits the normalized prompt-risk dataset, trains a DistilBERT classifier with PyTorch Lightning, logs metrics to W&amp;B, and saves model and metrics artifacts:</p> <pre><code>Input:\n  data/processed/prompt_risk_dataset.jsonl\n\nOutputs:\n  data/processed/{train,val,test}.jsonl\n  models/prompt_risk_distilbert/\n  reports/metrics.json\n</code></pre> <p>The training code is organized Lightning-style: <code>lightning_module.py</code> (model, loss, metrics), <code>lightning_data_module.py</code> (splits and dataloaders), and <code>train.py</code> driving <code>lightning.Trainer</code> from the Hydra config (<code>configs/train.yaml</code>).</p>"},{"location":"training/#hardware-profiles","title":"Hardware profiles","text":"<p>Hydra provides three hardware profiles (<code>configs/hardware/</code>):</p> <pre><code>local       one auto-selected device, 32-bit\nsingle_gpu  one CUDA GPU, 16-bit mixed precision\nddp         two CUDA GPUs on one node, 16-bit mixed precision (DDP)\n</code></pre> <pre><code>uv run python -m shapiq_attribution.train                      # defaults to local\nuv run python -m shapiq_attribution.train hardware=single_gpu\nuv run dvc repro train_vertex_ddp                              # canonical producer; needs two CUDA GPUs\n</code></pre>"},{"location":"training/#experiment-tracking","title":"Experiment tracking","text":"<p>Training logs to Weights &amp; Biases; <code>configs/sweep.yaml</code> defines a Bayesian hyperparameter sweep maximising <code>val/roc_auc</code>. In containers the W&amp;B API key can be injected from GCP Secret Manager (<code>WANDB_API_KEY_SECRET</code>), so no credentials live in code or images.</p>"},{"location":"training/#data-versioning","title":"Data versioning","text":"<p>DVC with a Google Cloud Storage remote (<code>gs://prompt_classifier_mlops</code>). Raw snapshots are tracked via <code>data/raw.dvc</code>; processed data, the served model and metrics are pipeline outputs declared in <code>dvc.yaml</code> and recorded in <code>dvc.lock</code>.</p>"},{"location":"training/#using-the-trained-classifier-from-python","title":"Using the trained classifier from Python","text":"<pre><code>from shapiq_attribution.model import PromptRiskPredictor\n\npredictor = PromptRiskPredictor.from_pretrained(\n    \"models/prompt_risk_distilbert\",\n    max_length=128,\n    device=\"cpu\",\n)\n\np_risky = predictor(\"masked prompt text\")  # masked prompt -&gt; P(risky)\n</code></pre> <p><code>PromptRiskPredictor</code> is also the value-function interface the attribution game evaluates on masked prompts \u2014 see the code reference.</p>"}]}